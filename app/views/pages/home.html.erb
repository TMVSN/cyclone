<h1>let's code !</h1>



<% if user_signed_in? %>

  <p>Current role :</p>
    <% if current_user.manager == true && current_user.fixer == false %>
    <p>Manager</p>
  <% elsif current_user.manager == false && current_user.fixer == true %>
    <p>Fixer</p>
  <% else %>
    <p>Customer</p>
  <% end %>

  <p><%= link_to "New User", new_user_path, class: "btn btn-success" %></p>
  <p><%= link_to "Users index", users_path, class: "btn btn-secondary" %></p>
  <p><%= link_to "New order", new_order_path, class: "btn btn-info" %></p>
  <p><%= link_to "Orders", orders_path, class: "btn btn-warning" %></p>
  <p><%= link_to "log out", destroy_user_session_path, class: "btn btn-primary" %></p>
<% else %>
  <%= link_to "Sign in", new_user_session_path, class: "btn btn-danger" %>
<% end %>


<%# Old code above %>

<% if user_signed_in? %>

  <p>Welcome home <%= current_user.first_name %></p>

    <%# manager %>
    <% if current_user.manager == true && current_user.fixer == false %>
      <p>Current role : Manager</p>

      <div class="home-content">
        <div class="home-card">
          <p><%= link_to "Orders", orders_path, class: "btn btn-warning" %></p>
        </div>
        <div class="home-card">
          <p><%= link_to "Users", users_path, class: "btn btn-secondary" %></p>
        </div>
      </div>

    <%# fixer %>
    <% elsif current_user.manager == false && current_user.fixer == true %>
      <p>Current role : Fixer</p>

      <div class="home-content">
        <div class="home-card">
          <p><%= link_to "My orders - TO DO", orders_path, class: "btn btn-warning" %></p>
        </div>
        <div class="home-card">
          <p><%= link_to "My Profile TO-DO", users_path, class: "btn btn-secondary" %></p>
        </div>
      </div>

    <%# customer %>
    <% else %>
      <p>Current role : Customer</p>

      <div class="home-content">
        <div class="home-card">
          <p><%= link_to "My orders - TO DO", orders_path, class: "btn btn-warning" %></p>
        </div>
        <div class="home-card">
          <p><%= link_to "My Profile TO-DO", users_path, class: "btn btn-secondary" %></p>
        </div>
      </div>

    <% end %>

  
<% else %>

  <%= link_to "Sign in", new_user_session_path, class: "btn btn-danger" %>


<% end %>